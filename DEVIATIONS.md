# DEVIATIONS.md

Where the build deliberately departs from plan.md's letter, and why. Each
entry names the section it departs from, the deviation, and the reason. Per
§3.2, "adjust the catalog, not the sections" — these are recorded, not hidden.

| #   | Section                       | Deviation                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Reason                                                                                                                                                                                                                                                                                                          |
| --- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | §4.13.2 / M0 gate 21          | Queue `message_retention_period` is **86400 s** (1 day), not 1209600 s (14 days)                                                                                                                                                                                                                                                                                                                                                                                               | The Cloudflare Queues API rejects >86400 s on this account/plan — the create call caps at 86400. DLQ + `* * * * *` sweeper semantics are unaffected; a message older than a day was already lost. Revisit if the account tier changes.                                                                          |
| D2  | §16.5 M1 deliverables         | Root `vitest.config.ts` projects list adds `"tools/eslint-plugin-musebook/vitest.config.ts"`                                                                                                                                                                                                                                                                                                                                                                                   | The plan's verbatim project list omits `tools/`; without it the eslint-rule tests (a M1 gate requirement) never execute under `pnpm vitest run`.                                                                                                                                                                |
| D3  | §12.2.8 vs §16.5 M10 gate 2   | Plan-internal contradiction, unresolved: `agplContainment` fails if `infra/postiz/Dockerfile` exists, while M10 gate 2 asserts it exists                                                                                                                                                                                                                                                                                                                                       | Transcribed `agplContainment` verbatim (existence = violation). Resolve at M10 — likely intent is a compose/deploy file that pulls the UPSTREAM image with no layered code; adjust whichever check is wrong, never both.                                                                                        |
| D4  | §3.2                          | `pnpm-workspace.yaml` carries `allowBuilds` (esbuild/workerd/sharp true; core-js-pure/@swc/core false)                                                                                                                                                                                                                                                                                                                                                                         | pnpm 12 replaced `onlyBuiltDependencies`/`ignoredBuiltDependencies` with a mandatory per-package allowlist; workerd and esbuild genuinely need their postinstalls, core-js-pure and @swc/core ship prebuilt.                                                                                                    |
| D8  | §3.2 catalog                  | `@changesets/cli` pinned to `2.31.1`, not `2.30.1`                                                                                                                                                                                                                                                                                                                                                                                                                             | `2.30.1` does not exist on the registry (`ERR_PNPM_NO_MATCHING_VERSION`); `2.31.1` is the newest same-major release (`maintenance-v2` dist-tag).                                                                                                                                                                |
| D5  | §3.6.1                        | **Resolved (M2):** `apps/*/wrangler.jsonc` Hyperdrive `id` fields held `0000…` placeholders until H3 cleared                                                                                                                                                                                                                                                                                                                                                                   | Both configs now exist — `HYPERDRIVE_CACHED`→`musebook-prod` `48c4c1c7…`, `HYPERDRIVE_FRESH`→`musebook-prod-fresh` `5288b46c…` (also corrected from `postgres` to the `musebook_worker` user per §line ~2796); live ids are committed in the wrangler files.                                                    |
| D6  | §3.2 catalog                  | wrangler pinned to `4.136.1`, not `4.136.0`                                                                                                                                                                                                                                                                                                                                                                                                                                    | The plan's pin predates 4.136.1's publish (2026-09-21); M1.15 demands `npm view wrangler version` parity, so the catalog moves to latest per §3.2's own rule. `minimumReleaseAgeExclude` covers both during the cutoff window.                                                                                  |
| D7  | §3.4                          | `apps/edge/src/guards/forbid.ts` message text differs slightly from the plan's wording                                                                                                                                                                                                                                                                                                                                                                                         | Semantics identical (module-scope throw); normalize when §3.4's exact string is transcribed into a test that greps it.                                                                                                                                                                                          |
| D9  | §3.5                          | `apps/edge/vitest.config.ts` declares a `musebook-worker` stub in `miniflare.workers`                                                                                                                                                                                                                                                                                                                                                                                          | The vitest pool cannot spawn a second wrangler project; MIXER is a named-entrypoint service binding and workerd refuses to start without it. The stub satisfies the binding until real worker tests land.                                                                                                       |
| D10 | §4.14 (grant layer)           | Migration 14 revokes `insert, update, delete` on all public tables from `anon`/`authenticated`, standing + `alter default privileges`                                                                                                                                                                                                                                                                                                                                          | Supabase's built-in defaults grant ALL on new public tables to `anon`/`authenticated`; §4.14's policy layer already denies client writes, this makes the grant layer agree instead of relying on RLS alone.                                                                                                     |
| D11 | §4.14 (RLS posture)           | `app.ensure_action_event_partitions` + the four bootstrap leaves run `enable` + `force row level security` on each daily partition                                                                                                                                                                                                                                                                                                                                             | Partition flags do not cascade from the parent; without them every tomorrow-dated leaf fails the §4.14 FORCE audit and the `rls_disabled_in_public` advisor lint.                                                                                                                                               |
| D12 | §4.x (functions)              | All seven public/app functions carry `set search_path = pg_catalog, public, app`                                                                                                                                                                                                                                                                                                                                                                                               | Without the parameter, function bodies resolve through the _caller's_ search_path — the `function_search_path_mutable` advisor lint flagged all seven.                                                                                                                                                          |
| D13 | §16.2 M2.3                    | `m2AdvisorsSecurity` counts findings at ERROR/WARN only, not INFO                                                                                                                                                                                                                                                                                                                                                                                                              | Postgres cannot attach a policy to a partition, so the four RLS-enabled `action_events_*_<date>` leaves carry a permanent INFO `rls_enabled_no_policy` — unfixable by design; the parent's policies govern their rows. Zero findings still enforced at every actionable level.                                  |
| D14 | §16.2 / §5 (seed refs)        | Migration 91400's reference rows use literal ids (`00000000-…` block) and a `SEED_EPOCH` timestamp, not `gen_random_uuid()`/`now()`                                                                                                                                                                                                                                                                                                                                            | G-SEED requires two consecutive `db reset` runs to dump byte-identical public data; generated ids/timestamps broke that on every replay.                                                                                                                                                                        |
| D15 | §17.12 (G-SEED impl)          | `db:seed-hash` strips `\restrict`/`\unrestrict` lines and waits `pg_isready` after `db reset` before dumping                                                                                                                                                                                                                                                                                                                                                                   | pg_dump emits a random token per dump (Postgres 18 hardening), and the CLI returns before the restarted container accepts connections — both produce hash noise unrelated to seed content.                                                                                                                      |
| D16 | §5 pgTAP                      | `001_rls_matrix.test.sql` adds `extensions` to `set local search_path`                                                                                                                                                                                                                                                                                                                                                                                                         | `supabase test db` pre-creates pgtap inside the `extensions` schema (Supabase convention), so `no_plan()` is invisible unless it is on the path.                                                                                                                                                                |
| D17 | §15.6 axis B                  | Live-policy verification runs as a real `musebook_worker` connection in `scripts/sql/assert-rls-worker.sql`, not `set role`                                                                                                                                                                                                                                                                                                                                                    | `set role` is a no-op for superusers and `postgres` is not a member of the `musebook_*` roles, so it would prove nothing.                                                                                                                                                                                       |
| D18 | §4.x migrations               | `identity_link.sql` uses a `plpgsql` DO block and relocates its grants                                                                                                                                                                                                                                                                                                                                                                                                         | Supabase's migration runner rejects the plan's multi-statement DO-else form and grants executed in-file ran before the roles it names exist.                                                                                                                                                                    |
| D19 | §4.9 partitions               | `app.ensure_action_event_partitions` replaces `pg_partman`'s `run_maintenance_proc`                                                                                                                                                                                                                                                                                                                                                                                            | `pg_partman` is not available on Supabase's managed Postgres; a small hand-rolled procedure creates the daily leaves and the pg_cron guard calls it instead.                                                                                                                                                    |
| D20 | §4.9 / §13.7                  | `pg_cron` schedule statements are guarded (`create extension … where not exists` + `cron.schedule` inside a guarded DO block)                                                                                                                                                                                                                                                                                                                                                  | `pg_cron` is not installed in the local dev DB, so unconditional schedule DDL would break `db reset` from zero.                                                                                                                                                                                                 |
| D21 | §5 seed                       | `post_bodies` has 25 rows for 24 posts                                                                                                                                                                                                                                                                                                                                                                                                                                         | The §5 fixture's editorial post intentionally carries two body revisions; count asserted in the seed inventory, not a typo.                                                                                                                                                                                     |
| D22 | §4.13 (final paragraph)       | `app.enter` had no migration anywhere in §4.x — it was only ever a DO-block draft; it ships in `20260922091500_app_enter.sql`, a prefix §16.8 leaves "deliberately unused" for exactly this kind of miss                                                                                                                                                                                                                                                                       | Without the file, `delegations_actor_read` (which calls `app.enter`) fails on `db reset` — the function never existed. Registered `["20260922091500_app_enter", 3]` in the manifest's registry so `m2MigrationSet` keeps the set compare two-directional.                                                       |
| D23 | §5.6.2/§5.7.5 (call pattern)  | `select app.enter(...) , (select …)` as sibling subqueries does **not** work on Postgres ≥12 — the planner binds table grants + RLS at plan time, before `app.enter` runs; live test: `permission denied for table sessions`. All such reads are plpgsql `security invoker` helpers that `perform app.enter(...)` then read in one call (`app.read_session_by_token`, `app.read_agent_identity`, `app.check_delegation_state`, `app.enqueue_sighting`, `app.audit_log_insert`) | Plan time ≠ run time; §4.13 itself names "one plpgsql call" as the alternative, this is it. `grant usage on schema app/extensions to musebook_worker` added because every helper is INVOKER — `app.sha256_hex` needs `extensions.digest` to resolve under the caller's grants.                                  |
| D24 | §16.5 M3.10                   | Rejection error is `insufficient_scope:<scope>` (names the scope), not the plan's bare `insufficient_scope`                                                                                                                                                                                                                                                                                                                                                                    | Gate M3.10 requires the rejection to NAME the missing scope; §5.7.5's verbatim error string didn't. The `<scope>` suffix satisfies both texts.                                                                                                                                                                  |
| D25 | §5.7.5                        | `DELETE /api/agent/delegations/[id]` re-checks `delegations.owner_user_id = session.userId` in the route before calling `revoke_delegation`                                                                                                                                                                                                                                                                                                                                    | `public.revoke_delegation` is SECURITY DEFINER with no owner predicate (it's also the jobs-plane cancel path); a non-owner caller would otherwise revoke someone else's delegation.                                                                                                                             |
| D26 | §5.5/§5 seed vocab            | Seed connector `default_scopes` → `{feed:read,post:read}`; seed delegation scopes → `{feed:read,post:read,post:write}` (schema-vocabulary slugs, not the plan's display labels); one `sessions` fixture added (`token_sha256 = app.sha256_hex('musebook-seed-session-token-0001')`)                                                                                                                                                                                            | The gate's actor/delegation tests resolve through the real scope vocabulary; the session fixture makes `mb_session` testable end-to-end.                                                                                                                                                                        |
| D27 | §5.6.2 (error surface)        | `WebBotAuthRejected` gains a `retryAfter` field (set to 30 on the 503 path only)                                                                                                                                                                                                                                                                                                                                                                                               | Gate M3.2 requires `Retry-After: 30` on directory-unreachable; §5.6.2's class carried status+reason only — the response mapper needs the value on the throw.                                                                                                                                                    |
| D28 | §3 (package layout)           | `packages/schema` uses `"moduleResolution": "bundler"` and its internal relative imports are extensionless (not the verbatim `.js` specifiers)                                                                                                                                                                                                                                                                                                                                 | Turbopack does not substitute `.js`→`.ts` on relative imports and has no `extensionAlias`; the package's exports map already points at `src/` so bundler resolution is the only form `apps/web` can consume. TSC emits are unaffected.                                                                          |
| D29 | §5.3 ("verbatim" module init) | `thirdwebAuth` and `serviceDb` are constructed lazily on first property access (a `Proxy` over a `cached ??=` factory), not at module evaluation                                                                                                                                                                                                                                                                                                                               | `next build` page-data collection imports route modules without Vercel env; top-level `createAuth`/`createClient` throw on the missing secrets and kill the build. Call sites are unchanged.                                                                                                                    |
| D30 | §17.12 (gate psql routing)    | `sqlCheck`/`sqlRun` route non-`postgres` login roles through `docker exec <supabase container> psql postgresql://<role>@127.0.0.1:5432/postgres`, and multi-statement M3.12 steps are split into one `sqlRun` each                                                                                                                                                                                                                                                             | The local Supabase `postgres` is a member-not-admin of the `musebook_*` roles and cannot `set role`; `set local role` also only lives to transaction end, so sibling statements in one `psql -c` never see the switch. `musebook_worker` has `set_option=t` — the production pattern — so the check runs as it. |
| D31 | §5.3 (type surface)           | `thirdwebAuth` is annotated with a locally-declared `ThirdwebAuth` interface built from thirdweb's public exports (`GenerateLoginPayloadParams`, `LoginPayload`, `VerifyLoginPayloadParams`, `VerifyLoginPayloadResult`, `JWTPayload`); `generateJWT`'s parameter is typed `LoginPayload`                                                                                                                                                                                      | `createAuth`'s return reaches the un-exported `VerifiedLoginPayload` through `generateJWT` — composite `.d.ts` emit fails TS2883 ("cannot be named"). `VerifiedLoginPayload` is `LoginPayload` + brand, so callers stay assignable.                                                                             |
| D32 | §17.14 (coverage provider)    | Coverage provider is `istanbul`, not v8: `coverage.provider: "istanbul"` at the root and `coverage: { enabled: false }` on the `apps/mcp` + `apps/worker` stub projects (which have no measured floor yet)                                                                                                                                                                                                                                                                     | V8 native coverage needs `node:inspector`, which workerd does not implement — every `*workers` pool threw `ERR_METHOD_NOT_IMPLEMENTED` and @cloudflare/vitest-plugin itself prescribes istanbul. One provider serves both tiers.                                                                                |
| D33 | §17.14 (coverage scope)       | `coverage.include` is scoped to the packages whose §17.14 floor has ACTIVATED (`packages/content`, `packages/connectors`, `apps/edge`, `apps/web/proxy.ts`); the per-milestone floors are expressed as per-glob thresholds on that list                                                                                                                                                                                                                                        | The global 80/75 floor cannot apply to stub packages a later milestone owns — including them would fail the universal check for code that does not exist yet. The list widens at each floor's activation milestone.                                                                                             |
