# DEVIATIONS.md

Where the build deliberately departs from plan.md's letter, and why. Each
entry names the section it departs from, the deviation, and the reason. Per
§3.2, "adjust the catalog, not the sections" — these are recorded, not hidden.

| #   | Section                                      | Deviation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Reason                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | §4.13.2 / M0 gate 21                         | Queue `message_retention_period` is **86400 s** (1 day), not 1209600 s (14 days)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | The Cloudflare Queues API rejects >86400 s on this account/plan — the create call caps at 86400. DLQ + `* * * * *` sweeper semantics are unaffected; a message older than a day was already lost. Revisit if the account tier changes.                                                                                                                                                                |
| D2  | §16.5 M1 deliverables                        | Root `vitest.config.ts` projects list adds `"tools/eslint-plugin-musebook/vitest.config.ts"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The plan's verbatim project list omits `tools/`; without it the eslint-rule tests (a M1 gate requirement) never execute under `pnpm vitest run`.                                                                                                                                                                                                                                                      |
| D3  | §12.2.8 vs §16.5 M10 gate 2                  | Plan-internal contradiction, unresolved: `agplContainment` fails if `infra/postiz/Dockerfile` exists, while M10 gate 2 asserts it exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Transcribed `agplContainment` verbatim (existence = violation). Resolve at M10 — likely intent is a compose/deploy file that pulls the UPSTREAM image with no layered code; adjust whichever check is wrong, never both.                                                                                                                                                                              |
| D4  | §3.2                                         | `pnpm-workspace.yaml` carries `allowBuilds` (esbuild/workerd/sharp true; core-js-pure/@swc/core false)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | pnpm 12 replaced `onlyBuiltDependencies`/`ignoredBuiltDependencies` with a mandatory per-package allowlist; workerd and esbuild genuinely need their postinstalls, core-js-pure and @swc/core ship prebuilt.                                                                                                                                                                                          |
| D8  | §3.2 catalog                                 | `@changesets/cli` pinned to `2.31.1`, not `2.30.1`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `2.30.1` does not exist on the registry (`ERR_PNPM_NO_MATCHING_VERSION`); `2.31.1` is the newest same-major release (`maintenance-v2` dist-tag).                                                                                                                                                                                                                                                      |
| D5  | §3.6.1                                       | **Resolved (M2):** `apps/*/wrangler.jsonc` Hyperdrive `id` fields held `0000…` placeholders until H3 cleared                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Both configs now exist — `HYPERDRIVE_CACHED`→`musebook-prod` `48c4c1c7…`, `HYPERDRIVE_FRESH`→`musebook-prod-fresh` `5288b46c…` (also corrected from `postgres` to the `musebook_worker` user per §line ~2796); live ids are committed in the wrangler files.                                                                                                                                          |
| D6  | §3.2 catalog                                 | wrangler pinned to `4.136.1`, not `4.136.0`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | The plan's pin predates 4.136.1's publish (2026-09-21); M1.15 demands `npm view wrangler version` parity, so the catalog moves to latest per §3.2's own rule. `minimumReleaseAgeExclude` covers both during the cutoff window.                                                                                                                                                                        |
| D7  | §3.4                                         | `apps/edge/src/guards/forbid.ts` message text differs slightly from the plan's wording                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Semantics identical (module-scope throw); normalize when §3.4's exact string is transcribed into a test that greps it.                                                                                                                                                                                                                                                                                |
| D9  | §3.5                                         | `apps/edge/vitest.config.ts` declares a `musebook-worker` stub in `miniflare.workers`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | The vitest pool cannot spawn a second wrangler project; MIXER is a named-entrypoint service binding and workerd refuses to start without it. The stub satisfies the binding until real worker tests land.                                                                                                                                                                                             |
| D10 | §4.14 (grant layer)                          | Migration 14 revokes `insert, update, delete` on all public tables from `anon`/`authenticated`, standing + `alter default privileges`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Supabase's built-in defaults grant ALL on new public tables to `anon`/`authenticated`; §4.14's policy layer already denies client writes, this makes the grant layer agree instead of relying on RLS alone.                                                                                                                                                                                           |
| D11 | §4.14 (RLS posture)                          | `app.ensure_action_event_partitions` + the four bootstrap leaves run `enable` + `force row level security` on each daily partition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Partition flags do not cascade from the parent; without them every tomorrow-dated leaf fails the §4.14 FORCE audit and the `rls_disabled_in_public` advisor lint.                                                                                                                                                                                                                                     |
| D12 | §4.x (functions)                             | All seven public/app functions carry `set search_path = pg_catalog, public, app`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Without the parameter, function bodies resolve through the _caller's_ search_path — the `function_search_path_mutable` advisor lint flagged all seven.                                                                                                                                                                                                                                                |
| D13 | §16.2 M2.3                                   | `m2AdvisorsSecurity` counts findings at ERROR/WARN only, not INFO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Postgres cannot attach a policy to a partition, so the four RLS-enabled `action_events_*_<date>` leaves carry a permanent INFO `rls_enabled_no_policy` — unfixable by design; the parent's policies govern their rows. Zero findings still enforced at every actionable level.                                                                                                                        |
| D14 | §16.2 / §5 (seed refs)                       | Migration 91400's reference rows use literal ids (`00000000-…` block) and a `SEED_EPOCH` timestamp, not `gen_random_uuid()`/`now()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | G-SEED requires two consecutive `db reset` runs to dump byte-identical public data; generated ids/timestamps broke that on every replay.                                                                                                                                                                                                                                                              |
| D15 | §17.12 (G-SEED impl)                         | `db:seed-hash` strips `\restrict`/`\unrestrict` lines and waits `pg_isready` after `db reset` before dumping                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | pg_dump emits a random token per dump (Postgres 18 hardening), and the CLI returns before the restarted container accepts connections — both produce hash noise unrelated to seed content.                                                                                                                                                                                                            |
| D16 | §5 pgTAP                                     | `001_rls_matrix.test.sql` adds `extensions` to `set local search_path`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `supabase test db` pre-creates pgtap inside the `extensions` schema (Supabase convention), so `no_plan()` is invisible unless it is on the path.                                                                                                                                                                                                                                                      |
| D17 | §15.6 axis B                                 | Live-policy verification runs as a real `musebook_worker` connection in `scripts/sql/assert-rls-worker.sql`, not `set role`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `set role` is a no-op for superusers and `postgres` is not a member of the `musebook_*` roles, so it would prove nothing.                                                                                                                                                                                                                                                                             |
| D18 | §4.x migrations                              | `identity_link.sql` uses a `plpgsql` DO block and relocates its grants                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Supabase's migration runner rejects the plan's multi-statement DO-else form and grants executed in-file ran before the roles it names exist.                                                                                                                                                                                                                                                          |
| D19 | §4.9 partitions                              | `app.ensure_action_event_partitions` replaces `pg_partman`'s `run_maintenance_proc`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `pg_partman` is not available on Supabase's managed Postgres; a small hand-rolled procedure creates the daily leaves and the pg_cron guard calls it instead.                                                                                                                                                                                                                                          |
| D20 | §4.9 / §13.7                                 | `pg_cron` schedule statements are guarded (`create extension … where not exists` + `cron.schedule` inside a guarded DO block)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `pg_cron` is not installed in the local dev DB, so unconditional schedule DDL would break `db reset` from zero.                                                                                                                                                                                                                                                                                       |
| D21 | §5 seed                                      | `post_bodies` has 25 rows for 24 posts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | The §5 fixture's editorial post intentionally carries two body revisions; count asserted in the seed inventory, not a typo.                                                                                                                                                                                                                                                                           |
| D22 | §4.13 (final paragraph)                      | `app.enter` had no migration anywhere in §4.x — it was only ever a DO-block draft; it ships in `20260922091500_app_enter.sql`, a prefix §16.8 leaves "deliberately unused" for exactly this kind of miss                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Without the file, `delegations_actor_read` (which calls `app.enter`) fails on `db reset` — the function never existed. Registered `["20260922091500_app_enter", 3]` in the manifest's registry so `m2MigrationSet` keeps the set compare two-directional.                                                                                                                                             |
| D23 | §5.6.2/§5.7.5 (call pattern)                 | `select app.enter(...) , (select …)` as sibling subqueries does **not** work on Postgres ≥12 — the planner binds table grants + RLS at plan time, before `app.enter` runs; live test: `permission denied for table sessions`. All such reads are plpgsql `security invoker` helpers that `perform app.enter(...)` then read in one call (`app.read_session_by_token`, `app.read_agent_identity`, `app.check_delegation_state`, `app.enqueue_sighting`, `app.audit_log_insert`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Plan time ≠ run time; §4.13 itself names "one plpgsql call" as the alternative, this is it. `grant usage on schema app/extensions to musebook_worker` added because every helper is INVOKER — `app.sha256_hex` needs `extensions.digest` to resolve under the caller's grants.                                                                                                                        |
| D24 | §16.5 M3.10                                  | Rejection error is `insufficient_scope:<scope>` (names the scope), not the plan's bare `insufficient_scope`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Gate M3.10 requires the rejection to NAME the missing scope; §5.7.5's verbatim error string didn't. The `<scope>` suffix satisfies both texts.                                                                                                                                                                                                                                                        |
| D25 | §5.7.5                                       | `DELETE /api/agent/delegations/[id]` re-checks `delegations.owner_user_id = session.userId` in the route before calling `revoke_delegation`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `public.revoke_delegation` is SECURITY DEFINER with no owner predicate (it's also the jobs-plane cancel path); a non-owner caller would otherwise revoke someone else's delegation.                                                                                                                                                                                                                   |
| D26 | §5.5/§5 seed vocab                           | Seed connector `default_scopes` → `{feed:read,post:read}`; seed delegation scopes → `{feed:read,post:read,post:write}` (schema-vocabulary slugs, not the plan's display labels); one `sessions` fixture added (`token_sha256 = app.sha256_hex('musebook-seed-session-token-0001')`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | The gate's actor/delegation tests resolve through the real scope vocabulary; the session fixture makes `mb_session` testable end-to-end.                                                                                                                                                                                                                                                              |
| D27 | §5.6.2 (error surface)                       | `WebBotAuthRejected` gains a `retryAfter` field (set to 30 on the 503 path only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Gate M3.2 requires `Retry-After: 30` on directory-unreachable; §5.6.2's class carried status+reason only — the response mapper needs the value on the throw.                                                                                                                                                                                                                                          |
| D28 | §3 (package layout)                          | `packages/schema` uses `"moduleResolution": "bundler"` and its internal relative imports are extensionless (not the verbatim `.js` specifiers)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Turbopack does not substitute `.js`→`.ts` on relative imports and has no `extensionAlias`; the package's exports map already points at `src/` so bundler resolution is the only form `apps/web` can consume. TSC emits are unaffected.                                                                                                                                                                |
| D29 | §5.3 ("verbatim" module init)                | `thirdwebAuth` and `serviceDb` are constructed lazily on first property access (a `Proxy` over a `cached ??=` factory), not at module evaluation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `next build` page-data collection imports route modules without Vercel env; top-level `createAuth`/`createClient` throw on the missing secrets and kill the build. Call sites are unchanged.                                                                                                                                                                                                          |
| D30 | §17.12 (gate psql routing)                   | `sqlCheck`/`sqlRun` route non-`postgres` login roles through `docker exec <supabase container> psql postgresql://<role>@127.0.0.1:5432/postgres`, and multi-statement M3.12 steps are split into one `sqlRun` each                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | The local Supabase `postgres` is a member-not-admin of the `musebook_*` roles and cannot `set role`; `set local role` also only lives to transaction end, so sibling statements in one `psql -c` never see the switch. `musebook_worker` has `set_option=t` — the production pattern — so the check runs as it.                                                                                       |
| D31 | §5.3 (type surface)                          | `thirdwebAuth` is annotated with a locally-declared `ThirdwebAuth` interface built from thirdweb's public exports (`GenerateLoginPayloadParams`, `LoginPayload`, `VerifyLoginPayloadParams`, `VerifyLoginPayloadResult`, `JWTPayload`); `generateJWT`'s parameter is typed `LoginPayload`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `createAuth`'s return reaches the un-exported `VerifiedLoginPayload` through `generateJWT` — composite `.d.ts` emit fails TS2883 ("cannot be named"). `VerifiedLoginPayload` is `LoginPayload` + brand, so callers stay assignable.                                                                                                                                                                   |
| D32 | §17.14 (coverage provider)                   | Coverage provider is `istanbul`, not v8: `coverage.provider: "istanbul"` at the root and `coverage: { enabled: false }` on the `apps/mcp` + `apps/worker` stub projects (which have no measured floor yet)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | V8 native coverage needs `node:inspector`, which workerd does not implement — every `*workers` pool threw `ERR_METHOD_NOT_IMPLEMENTED` and @cloudflare/vitest-plugin itself prescribes istanbul. One provider serves both tiers.                                                                                                                                                                      |
| D33 | §17.14 (coverage scope)                      | `coverage.include` is scoped to the packages whose §17.14 floor has ACTIVATED (`packages/content`, `packages/connectors`, `apps/edge`, `apps/web/proxy.ts`); the per-milestone floors are expressed as per-glob thresholds on that list                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | The global 80/75 floor cannot apply to stub packages a later milestone owns — including them would fail the universal check for code that does not exist yet. The list widens at each floor's activation milestone.                                                                                                                                                                                   |
| D34 | §3.4 / §16.5 M5.8                            | The containment allow-list gains `packages/content/test/` and `packages/x402/test/` (dir-level, matching the rule's own `packages/kernel/test/` exemption)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | The §16.5 grep is verbatim but its §3.4 allow-list already exempts kernel test fixtures: a fixture _declares_ a mode value for a producer's input, it never _reads_ it. Content's `test/fixture.ts` builds a full `Resource` for representation tests; without the exemption the only compliant form is a grep-dodging computed key. File/dir-level only — any new hit outside test dirs still fails. |
| D35 | §6.5 (`resolveAccessWith`)                   | `return await charge(...)`, not the plan's bare `return charge(...)`, on both dispatch arms                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | An un-awaited `return` lets a rejecting `charge()` bypass the enclosing try/catch — a throwing PaymentPort would reject `resolveAccess` instead of producing `kernel_error`, violating the fail-closed invariant the same section asserts (M5.4 requires `allow:false` on a throwing port).                                                                                                           |
| D36 | §6.3 (deliverable list)                      | `packages/kernel` also ships `render/` modules (`index,html,markdown,json,jsonld,mcp,feed,preview`) authored to the §6.6 spec tables — the plan's file list names `renderResource` but not its internal decomposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | One `renderResource` entry point per §6.2; the per-representation producers are private modules so `jsonld.ts`/`preview.ts` stay plan-verbatim.                                                                                                                                                                                                                                                       |
| D37 | §16.8 (migration assignment)                 | `app.read_slate` + `app.finish_job` ship at M6 in `20260922092001_edge_read.sql` (verbatim §9.7 / §4.13); `20260922092002_telemetry_salts.sql` ships at M6, not M11                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Both are needed by M6 code — the feed route performs the one slate read, every queue consumer finishes through finish_job, and /api/events hashes client IPs through the daily salt. Later files re-create the same functions with `create or replace`, harmlessly.                                                                                                                                   |
| D38 | §4.12 / §13.2.3 / §17.11.5                   | `ops_events` gains `subject_id text` (+ index); `telemetry_salts` grants extend to `update, delete` on `musebook_worker` + `musebook_jobs` with `for all` RLS policies                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | §17.11.5's verbatim DLQ test queries `ops_events where subject_id = $1` and no §4 table declares it. §13.9.2's salt.ts runs `insert … on conflict do update` as musebook_worker (needs UPDATE) and `app.telemetry_rotate_salt` deletes (needs DELETE); RLS on a policy-less table would deny every access.                                                                                            |
| D39 | §16.8 / §5.7.5 (definer reads)               | `20260922092004_edge_helpers.sql` ships the ~28 plpgsql helpers M6's Worker + routes call (`app.list_author_posts`, `app.author_profile`, `app.list_authors`, `app.lookup_asset_by_key`, `app.ingest_action_events`, `app.resolve_slate_versions`, `app.adjust_counter`, `app.write_reverse_chron_slate`, `app.enqueue_job`, `app.sweep_outbox`, `app.claim_outbox_job`, `app.existing_embeddings`, `app.existing_classifications`, `app.record_embeddings`, `app.record_classifications`, `app.record_distribution_jobs`, `app.mark_outbox_enqueued`, `app.write_ops_event`, `app.reverse_chron_page`, `app.read_slate_doc`, `app.find_live_grant`, `app.mint_grant`, `app.pin_quote`, `app.is_agent_blocked`, `app.load_resource_by_slug`/`_by_post_id`, `app.list_public_resources`); three are `security definer` (`list_author_posts`, `author_profile`, `list_authors`) because they join kernel-only `wallets`/`creator_publishing_defaults` | Same D23 rule: member-role grants bind only after `app.enter`; catalog queries that must see kernel tables need the definer arm inside one function. `list_authors`/`author_profile` leak wallet addresses to the public read path by design — §7.13's author feed/llms surface displays payout addresses verbatim.                                                                                   |
| D40 | §13.4.4 / §13.9 / test envs / wrangler types | (a) M11 facet columns (anon_id, view_session_id, content_hash, outcome, max_scroll_pct, completion_pct) ride inside `action_events.client` until `20260922092000_telemetry_facets.sql` — the writer omits the columns, per §6.15; anonymous human batches are AE-only because `action_events_plane_purity` has no anon arm yet. (b) Miniflare test bindings set `X402_MODE=live` (prod `shadow`) so §17.11's hfap/agent 402 arms execute. (c) `wrangler types` marks every binding `?` optional under `env.preview`; `db/client.ts` exports `bound(v, name)` (throws "binding X is not configured") — the repo-wide read pattern, never `env.X!`. (d) `returns table (position …)` requires the quoted `"position"` — unquoted is a syntax error in the RETURNS TABLE grammar.                                                                                                                                                                      | (a) Forward-compat without inventing schema; (b) the gate tests the live decision, not the shadow write; (c) the generated type is the truth — narrowing helpers beat lying with `!`; (d) verbatim corrected where the SQL itself could not parse.                                                                                                                                                    |
| D41 | §15 (cache ruleset)                          | `cache-next-image` ships without `custom_key`; the default cache key (host + path + full query) already isolates `/_next/image`'s url/w/q parameters                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `custom_key` is an Enterprise-tier entitlement — the zone is Free. The deception-armor part (`cache_key.cache_deception_armor`) shipped fine; the key customization is what the entitlement gates.                                                                                                                                                                                                    |
| D42 | §15 / §6.12.6                                | No pay-per-crawl fence is written to the zone; `configure-cf.ts` carries a PPC **absence probe** (GETs on `ai_crawl`/`aibot`/`ppc`/`pay_per_crawl`/`ai_crawl_control` surfaces — all must stay 7003/404)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `pay_per_crawl` is not a real `http_config_settings` field — the API rejects it 400 and PPC is closed beta with no zone endpoint at all. The probe makes a future endpoint surfacing visible as drift instead of silently absent.                                                                                                                                                                     |
| D43 | §15 (expression operators)                   | Cache-rule expressions use `starts_with(http.request.uri.path, ...)`/`http.host eq`/`len(headers[...])>0`, never `matches`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `matches` (regex) needs Business/WAF Advanced on this zone — verified against the API. The prefix form expresses the same three paths.                                                                                                                                                                                                                                                                |
| D44 | §6.12.4 (media read)                         | `if-match`, `if-modified-since` and `if-unmodified-since` also route to `bucket.get(onlyIf: headers)` — not only `range`/`if-none-match`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | The `ctx.exports.PaidMedia` path cannot express a precondition; without this those headers silently served 200 instead of 412.                                                                                                                                                                                                                                                                        |
| D45 | §16.5 M6.7/M6.8 (check scope)                | The coverage sqlCheck scopes to `viewer_user_id is not null`; the density sqlCheck scopes to `action='impression'`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Anonymous slates are AE-only by §13.4.4 design — a viewer-scoped invariant asserted on them would fail a correct system. Position semantics exist only on impression rows (dwell/view carry their own fields).                                                                                                                                                                                        |
| D46 | §13.4 test fixtures                          | `wire.test.ts` seeds two `sessions` rows (`musebook-gate-slate-session-0001`, `musebook-seed-session-token-0001`) so the authed-batch arm resolves a real session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | The gate's own D26 session fixture only covered one token; the slate-coverage tests need a second viewer-owned session distinct from the first.                                                                                                                                                                                                                                                       |
| D47 | §17 (root vitest)                            | `onUnhandledError` stringifies non-Error rejections via JSON and also drops `"SpanParent"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | workerd teardown rejections reach the root handler as deserialized plain objects — `String(e)` produced `"[object Object]"` (and throws outright on null-prototype objects), which wedged the reporter and hung the gate. The `SpanParent` error is the same artifact class as "socket has been closed": the waitUntil'd `db.end()` orphan span firing after its request context ends.                |
| D48 | §7.12 PostView                               | `publishMode` removed from `postViewSchema`; `loadPostView` no longer selects `publish_mode` and returns null when the twin is absent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | §3.4's allow-list is verbatim file-scoped — plan §6.1/§3.4 names `kernel.ts` + `database.types.ts` and explicitly never a third schema file; the page only needs `accessBadge` and the twin is load-bearing by the same section.                                                                                                                                                                      |
| D49 | §3.2 (zod split) + workerd type edge         | `feed.ts` parses its POST body with a hand-rolled `parseBody`; `ctx.exports.PaidMedia` is used as `Fetcher`; `rangeHeaders` reads `R2Range` through an optional-keys runtime shape                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `import "zod"` is banned in apps/edge wholesale (zod-3 rides transitively through @x402/core). `ctx.exports`' static type depends on generated `GlobalProps` that the lint program does not resolve; Miniflare's R2Range instance carries `suffix: undefined` — the declared union models only the request-literal shape.                                                                             |
| D50 | §4.14 / §16.5 (grant matrix fixes)           | `edge_helpers.sql` revokes EXECUTE on every `app.*` from `public, anon, authenticated` (PG defaults grant it); `app.list_authors` drops its `app.enter` (SET ROLE is illegal inside `security definer` — the definer's owner IS the plane switch); `app.mint_grant` uses bare `on conflict do nothing` + `access_grants.`-qualified RETURNING                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | PUBLIC exec would bypass §4.14's whole matrix. `on conflict on constraint access_grants_payer_hash_uniq` fails at runtime — it is a partial unique _index_, and the `(payer, content_hash)` arbiter is ambiguous against the function's own OUT params. All three were found by gate coverage tests, not review.                                                                                      |
| D51 | §17 gate fixtures                            | coverage.test.ts event batches post on `...0001` (seed-note-free), not FREE_POST                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | wire.test.ts's DNT check diffs a global count filtered by post_id — concurrent batches on the same post race its window across files.                                                                                                                                                                                                                                                                 |
| D52 | §17 env stubs                                | proxy.test.ts stubs secrets with `undefined`, never `""`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `""` exercises nothing — `?? ""` arms only run when the env var is _absent_; stubEnv keeps the var defined on empty input.                                                                                                                                                                                                                                                                            |

## M7 — compose, publish, mode picker, media upload

| #   | Plan section              | Deviation                                                                                                                                                                                                                                                                                   | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D53 | §17 e2e harness           | The Playwright e2e suite runs `next start` (production), never `next dev`. `webServer[0]` is `next start -p 3310`, `webServer[1]` is `wrangler dev`, both probed on `/reels` (the probe URL must return 2xx — `/security.txt` 404s made the wait hang).                                     | next dev's Turbopack HMR client opens a `/_next/hmr` WebSocket the workerd fetch proxy cannot upgrade, so hydration never runs through the edge — the page renders with zero interactivity and no error. Production has no HMR channel.                                                                                                                                                                                                           |
| D54 | §6.12.2 (toOrigin)        | `next.config.ts` carries `experimental.serverActions.allowedOrigins = [musebook.dev, www.musebook.dev, 127.0.0.1:8787]`.                                                                                                                                                                    | Server Actions verify `Origin` against `x-forwarded-host`; behind the Worker the forwarded host is always the internal origin (`*.vercel.app` in prod), so every proxied action aborted `Invalid Server Actions request`. Required in production, not a test shim.                                                                                                                                                                                |
| D55 | §11.7.3 (uploads)         | `parseInit` accepts `postId: null` (the `InitBody` type already declared it); the first compose's upload inits before any draft exists.                                                                                                                                                     | Every `/api/uploads/init` with no saved draft 400'd on the `typeof o.postId !== "string"` guard.                                                                                                                                                                                                                                                                                                                                                  |
| D56 | §11.7.3 (bucket CORS)     | `musebook-uploads` CORS adds `http://127.0.0.1:8787` + `http://localhost:8787` to the allowed origins and exposes `etag`/`ETag`.                                                                                                                                                            | The e2e PUT is a browser request to the presigned URL from the wrangler dev origin; without the origin + exposeHeaders the part upload fails CORS and the multipart ETag never reaches complete.                                                                                                                                                                                                                                                  |
| D57 | §14.4.5 (composer)        | `apps/web/app/providers.tsx` wraps the tree in `<ThemeProvider>` + `<ThirdwebProvider>` as the single client boundary, wired from the root layout.                                                                                                                                          | `/compose` 500'd with `No QueryClient set` — the wallet picker needs thirdweb's QueryClient and no provider existed. §14.2.4's one-boundary rule is satisfied by the same file.                                                                                                                                                                                                                                                                   |
| D58 | §4.14 (grant matrix)      | `app.staged_upload_target(text)` is granted to `musebook_worker` in addition to `musebook_jobs`.                                                                                                                                                                                            | The sign-part route reads the staged row directly on the Worker plane (it is not a jobs-plane call); the consumer still reads it under `musebook_jobs`. NOINHERIT means a missing direct grant is a hard 42501 — surfaced as `/api/uploads/sign-part` 500s after `db reset` dropped the earlier hand-added grant.                                                                                                                                 |
| D59 | §16.5 seed                | The shared seed body sentence gains a second clause so `MUSEBOOK_PAID_BODY_MARKER_*` lands strictly past the 400-char preview boundary (marker now at char 428).                                                                                                                            | The original filler left the marker at char 355 < 400, so `previewOf` legitimately included it — the gated-body check failed on a seed bug, not a leak.                                                                                                                                                                                                                                                                                           |
| D60 | §6.12.2 (origin contract) | `renderedToResponse` sets `content-type: rendered.mediaType`; `fetchJsonTwin` forwards the inbound `cookie` header on the server-side twin fetch; `loadPostView` selects only real `posts` columns plus a parallel `profiles` row (no `revenue_share_version`, no denormalized `author_*`). | (a) `headersFor` never emitted content-type — every kernel-rendered body went out `text/plain`, breaking both the browser paywall render and the axe document checks. (b) Without the cookie the twin resolves anonymous and an author's own gated post 402s inside the page loader → 404. (c) `posts` has no `revenue_share_version`/`author_display_name`/`author_handle` columns — PostgREST 400'd the whole select → every `/p/{slug}` 404'd. |
| D61 | §14.5.4 (a11y)            | The mode-picker trigger renders `AccessBadge` non-interactive (a `<span>` inside the one `<button aria-haspopup="dialog">`).                                                                                                                                                                | `AccessBadge interactive` renders its own `<button>` — nested-interactive is a serious axe violation.                                                                                                                                                                                                                                                                                                                                             |
| D62 | §3.7 (env manifest)       | `ORIGIN_SCHEME` added to the manifest, wrangler.jsonc vars (`https`), and `worker-configuration.d.ts` regenerated.                                                                                                                                                                          | `toOrigin()` already read it with a `?? "https"` fallback; G-ENV/G-BOUND require every read documented and bound.                                                                                                                                                                                                                                                                                                                                 |

## M8 — x402 v2 paywall, grants, ledger, paid-media gate

| #   | Plan section              | Deviation                                                                                                                                                                                                                                                                  | Reason                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D63 | §17 e2e durability        | `scripts/test/ensure-worker-role.mjs` (the every-tier globalSetup) also asserts `max_connections = 500` on the local postgres container — writes `zz-gate.conf` to `postgresql.conf.d` and restarts the container when below 200.                                          | The M8.2 wire replay fans one signed authorization to 101 concurrent `SELF.fetch`es, each opening its own connection — Supabase's stock 100 (≈97 usable after reserved superuser slots) exhausts mid-suite. `supabase db reset` recreates the container and drops any manual conf, so the fix must live in setup, not the container. |
| D64 | §6.9/§6.10 (replay claim) | `insert_pending_settlement` claims `(network, asset, payer, nonce)` via `x402_settlements_replay_uniq` and surfaces mid-flight replays as `replay_in_flight`; a replayed `extra.quoteId` resolves to a _consumed_ quote → `quote_mismatch` → fresh 402 re-challenge.       | The SQL-tier test proves the atomic nonce claim (exactly-1 settlement across a 101-way race); wire replays arrive after the winner settles → the honest observable verdict is 402-with-fresh-quote (and 409 in-flight), not a second settle.                                                                                         |
| D65 | §6.7 (verify errors)      | The wrong-domain wire case accepts `invalid_exact_evm_signature` from x402.org; the plan's enumeration names `invalid_exact_evm_payload_signature`.                                                                                                                        | Live facilitator returns the former; the test asserts `/invalid_exact_evm/` so either spelling is caught by the arm it documents.                                                                                                                                                                                                    |
| D66 | §6.10 (legs dedupe)       | `post_settlement_legs` keeps `on conflict do nothing`; the arbiter is a new `unique index payout_ledger_tx_kind_uniq on (ledger_tx_id, account_kind)`.                                                                                                                     | Without a unique constraint `do nothing` has nothing to dedupe against — a reconciler re-post duplicated all three legs (found by the 3-row assertion, not review).                                                                                                                                                                  |
| D67 | §6.10 (platform leg)      | The platform leg's `account_address` is the settlement's own `pay_to` (subquery inside the insert), not a parameter.                                                                                                                                                       | The treasury IS the platform fee's beneficiary — taking it as a param lets a caller steer the fee elsewhere; reading it off the row keeps §6.10's "fee goes where the payment went" invariant in SQL.                                                                                                                                |
| D68 | §6.9 (settlement sketch)  | `x402_settlements` gains `payment_payload jsonb` — the verbatim signed payload the worker reconciler needs to re-send a facilitator settle that never broadcast. Also `revenue_share_policies` is `force row level security` (§4.14's owner-bypass rule the sketch omits). | §6.8's pending-row sweep can't retry what it can't rebuild; a `verify_only` resend would settle a different authorization. The force-RLS fix was caught by G-ROLE.                                                                                                                                                                   |
| D69 | §3.7/§17 (env manifest)   | `X402_FACILITATOR_URL` scope widened to `["E","M","J","L"]`; `X402_TEST_PAYER_KEY` added as `["L","CI"]` secret.                                                                                                                                                           | The worker reconciler (`J`) builds its facilitator client from the row's URL binding; the wire suite's live testnet payer key is a local/CI-only credential (a funded Base Sepolia EOA), never a deployed secret.                                                                                                                    |
| D70 | §16.5 (seed determinism)  | `revenue_share_policies.effective_from` pinned to `2026-09-01 00:00:00+00` in the seeding insert rather than relying on `default now()`.                                                                                                                                   | `now()` makes two seeded dumps differ row-for-row — G-SEED's identical-dump-hash invariant fails on the default. The migration column default stays for real policies added later.                                                                                                                                                   |
| D71 | §6 (actor.payment)        | `settleOnce` guards `actor.payment` with `== null`, not `=== null`.                                                                                                                                                                                                        | The field is optional on the Actor (`undefined` for a human with no PAYMENT-SIGNATURE); the strict guard fell through to a `TypeError` reading `.x402Version`.                                                                                                                                                                       |
| D72 | §14.2.3 (theme/a11y)      | `html` gets `background-color: var(--background)`; `ConnectButton` carries `connectButton={{ label: "Sign in" }}`.                                                                                                                                                         | The sticky header's `bg-background/80` blends over an otherwise-transparent root — a checker reading the blend sees light text on a light canvas; thirdweb's hydration placeholder renders a disabled button with no accessible name.                                                                                                |

## M9 — musebook-mcp, connector registry, agent planes

| #   | Plan section                 | Deviation                                                                                                                                                                                                                                                                                                                                                                                            | Reason                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D73 | §16.5 M9.2 / §7.11           | No `jwks.json` route is served; the OAuthProvider self-signs and serves RFC 9728 PRM itself via its `resourceMetadata` option (`/.well-known/oauth-protected-resource*`). `MCP_JWT_KID`/`MCP_JWT_PUBLIC_JWKS` removed from wrangler vars and the env manifest.                                                                                                                                       | Plan line 8356 deletes `MCP_JWT_*` entirely — JWKS was the superseded draft. The provider intercepts the well-known path before `defaultHandler`, so a manual route would be dead code.                                                                                                                                                                                              |
| D74 | §7.6.4 (anonymous reads)     | `apps/mcp/src/index.ts` wraps the OAuthProvider: requests to `/mcp` _without_ a `Bearer` header bypass `provider.fetch` and go straight to the MCP handler; anything else (and every other apiRoute request) goes through the provider.                                                                                                                                                              | `handleApiRequest` in workers-oauth-provider 401s ANY apiRoute request lacking `Bearer ` — anonymous x402 reads ("an anonymous agent with a wallet can buy and read") are impossible through it. The wrapper preserves the provider's 401 on non-mcp apiRoutes and all of /authorize, /register, /token.                                                                             |
| D75 | §7.20.27 (startup budget)    | `wrangler check startup` measures the profile window (43.2ms observed) instead of reading `startup_time_ms` out of `wrangler deploy --dry-run`; the check strips `hyperdrive` bindings from a scratch config since the profiler cannot reproduce them locally.                                                                                                                                       | Wrangler 4.x removed `startup_time_ms` from dry-run output; `check startup` is its successor. The bindings play no part in module evaluation — stripping them measures the same startup phase.                                                                                                                                                                                       |
| D76 | §7.20.20 (maxTimeoutSeconds) | The gate greps `maxTimeoutSeconds\s*[:=]\s*\d+` (literals only) rather than every `maxTimeoutSeconds` token.                                                                                                                                                                                                                                                                                         | The spec's `grep -vc 60` form counts identifier references and type declarations as violations; the intent is that no literal other than 60 appears. Variable references to the constant are what the constant is for.                                                                                                                                                               |
| D77 | §7.9 (OAuth dance)           | `POST /authorize` answers `302 → redirect_uri?code=...` — acceptance tests fetch with `redirect: "manual"` and read the `location` header; public clients use `token_endpoint_auth_method: "none"` at `/register` plus PKCE S256.                                                                                                                                                                    | fetch's default redirect mode follows the 302 to a nonexistent `/cb`; the provider (correctly) refuses Basic+client_id body auth together ("Client must not use multiple authentication methods") and requires PKCE for public clients — the spec's public-client profile, not a server quirk.                                                                                       |
| D78 | §10.7 (SQL typing)           | `app.actor_delegation` selects `d.state::text`; `revoke_delegation`, `app.sweep_stale_holds` and `app.set_follow` cast audit_log actor literals `::actor_class`.                                                                                                                                                                                                                                     | Enum `delegation_state`/`actor_class` does not coerce into a `text`/enum-returning function signature — `42804 structure of query does not match`. The 91700 draft missed the casts 90210 established elsewhere.                                                                                                                                                                     |
| D79 | §10.7.5 (revoke grants)      | `public.release_agent_spend` is also granted to `musebook_jobs`.                                                                                                                                                                                                                                                                                                                                     | `app.sweep_stale_holds` performs `app.enter('musebook_jobs')` before calling it — EXECUTE is checked against the current role, so the worker-plane grant alone denied the sweep.                                                                                                                                                                                                     |
| D80 | §16.5 M9.10 (gate)           | `vitestSlice` runs every invocation through `boundedRun` — `setsid` + output file + TERM/KILL of the whole process group at 300 s — and, when killed after a complete result printout, judges by the `✓ file`/`Test Files … passed` lines rather than the exit code and, when killed after a complete result printout, judges by the `✓ file`/`Test Files … passed` lines rather than the exit code. | The vitest reporter intermittently hangs after printing results on this stack (observed on the mcp acceptance suite under `-t` filtering) — `timeout`/execSync-timeout cannot help — the signal reaches only the shell while workerd grandchildren keep the stdio pipe open — so the group must be killed; a passed suite must not report FAIL; a passed suite must not report FAIL. |
| D81 | §7.20.16 (WebMCP spec)       | The no-provider spec filters the browser's `Failed to load resource: status 402` console line.                                                                                                                                                                                                                                                                                                       | Chromium logs every non-2xx subresource as a console error — the 402 IS the paywall working as intended; asserting zero console errors counts the success path as a failure.                                                                                                                                                                                                         |
| D82 | §6.12.8 (deny headers)       | The x402 deny branch in `apps/edge/src/routes/twin.ts` merges `usageHeadersFor(resource)` onto `paymentRequiredHttp`'s response.                                                                                                                                                                                                                                                                     | §7.20.21 reads `Content-Usage` off the wire for a denied fetch — the stored license is declared on the paid preview too, not only after payment.                                                                                                                                                                                                                                     |
| D83 | §10.8.4 (signature-agent)    | `SIGNATURE_AGENT_ENTRY` is `musebook="https://…/http-message-signatures-directory";type=directory` — the URI is a bare string, no `<>`.                                                                                                                                                                                                                                                              | web-bot-auth's own parser rejects the angle-bracketed form (it is not Structured Fields inner-list syntax) — `signOutbound` threw on every call; directory entries would never validate.                                                                                                                                                                                             |
| D84 | §10.6.3 (bridge idempotency) | `app.bridge_result_once` qualifies its `returning` as `idempotency_keys.response_body`.                                                                                                                                                                                                                                                                                                              | The OUT param `response_body` collided with the column reference — `42702 column reference is ambiguous` on the first real call.                                                                                                                                                                                                                                                     |
| D85 | §10.7 (draft context)        | `app.delegation_for_drafting` is granted to `musebook_jobs` in addition to `musebook_worker`.                                                                                                                                                                                                                                                                                                        | `app.bridge_task_context` calls it after `app.enter('musebook_jobs')` — same EXECUTE-under-current-role hole as D79; EXECUTE is checked against the jobs role, not the caller's.                                                                                                                                                                                                     |
| D86 | §16.5 (test pool teardown)   | Request-scoped `sql.end()` in `apps/mcp` (oauth.ts) and the pg stub is fire-and-forget (`void …catch()`) rather than awaited in a `finally`.                                                                                                                                                                                                                                                         | An awaited pool teardown on a request whose response already returned pins the workerd io-context: vitest's pool shutdown hung indefinitely. The fix matches D80's root-cause work — the socket drain belongs to the connection lifecycle, not the request's.                                                                                                                        |
| D87 | §4.14 (grant matrix)         | `scripts/sql/assert-rls-worker.sql` declares `creator_publishing_defaults` for the jobs + public_reader planes and `assets` for public_reader — the grants 91700 added are now in the assert matrix.                                                                                                                                                                                                 | The matrix was the only place the M9 grants weren't declared; `assets` skipped the flag only because the seed table is empty — declared anyway so the assert stays the declaration, not the accident.                                                                                                                                                                                |
| D88 | §16.5 (test isolation)       | `gate.test.ts` clears the seed crawler's `access_grants` on `SLUG.hfap`/`SLUG.gated` in `beforeAll`.                                                                                                                                                                                                                                                                                                 | m8-wire's hfap settle mints a DURABLE grant that outlives the run — a second local gate run read 200 instead of 402. CI (fresh reset) never sees it; the file now resets the fixture it asserts on, the same hazard m8-wire's own beforeAll already documents.                                                                                                                       |

## M10 — Postiz sidecar + distributor

| #    | Plan section                 | Deviation                                                                                                                                                                                                                                                                                                                                                | Reason                                                                                                                                                                                                                                                                                              |
| ---- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D89  | §12.2.8 / G-AGPL             | `infra/postiz/Dockerfile` is a single `FROM ghcr.io/gitroomhq/postiz-app@sha256:01b24a4f…` line (digest-pinned upstream image, no copies). `agplContainment` asserts that exact shape instead of asserting no Dockerfile, and its import regexes now require the specifier's first char to be non-dot and symbols to sit inside an import/export clause. | The gate demanded both "no Postiz source in the bundle" AND "Dockerfile exists" — the FROM-only pin satisfies the containment intent (zero upstream code in our bundle). The old regexes false-positived on our own `./postiz/` client module and on wire-strings like `"PostValidationException"`. |
| D90  | §13.3 / §6.3                 | `variantIntentFor` always returns `"full"` — paid posts port in full like any other; the `teaser` union member stays so the decision can reverse.                                                                                                                                                                                                        | CF-SPINE §13.3 supersedes the §6.3 sketch (paid → teaser): the paywall lives on the canonical URL and every variant links back to it.                                                                                                                                                               |
| D91  | §12.2.6 (webhook route)      | `postiz-webhook.ts` validates the delivery payload with a hand-rolled parser, not zod.                                                                                                                                                                                                                                                                   | §3.2 bans zod from `apps/edge`; the edge zod check enforces it repo-wide. A 40-line structural validator covers the two fields the route consumes (`postizPostId`, `state`).                                                                                                                        |
| D92  | §12.3.5 (variant upsert key) | `platform_variants` upserts on `(post_version_id, platform)` — its real unique constraint — not `channel_id`.                                                                                                                                                                                                                                            | One variant per platform per version is the invariant; two channels on the same platform share a variant. An upsert keyed by channel_id cannot express the table's own uniqueness.                                                                                                                  |
| D93  | §12.3.7 (gated signal)       | The distribute page derives `priced` from `price_atomic > 0` instead of reading `publish_mode`.                                                                                                                                                                                                                                                          | `no-publish-mode-outside-kernel` bans the literal outside `packages/kernel`; priced is the only signal the gate banner needs and the atomic column carries it.                                                                                                                                      |
| D94  | §12.3.4 (stage retry)        | A stage retry is a fresh outbox row whose dedupe key gains `:a${attempt+1}`; the attempt counter doubles as the Postiz 429 backoff signal, capped at `STAGE_MAX_ATTEMPTS=4`.                                                                                                                                                                             | The sketch's single-row retry collides with `job_outbox`'s dedupe uniqueness — requeueing a finished dedupe key is a silent no-op. The suffix keeps each attempt's enqueue observable in the outbox.                                                                                                |
| D95  | §16.6 (file layout)          | The distribute consumer lives at `apps/worker/src/consumers/distribute.ts`, not `src/queue/distribute.ts`; `POSTIZ_URL`/`POSTIZ_API_KEY`/`POSTIZ_WEBHOOK_SECRET` gained `"E"` (edge) scope for `channels-sync`/`postiz-webhook`/`distribute` routes.                                                                                                     | `consumers/` is where every queue dispatch branch already lives; the edge plane calls the Postiz API directly for sync + webhook + review actions, so the edge scope is real, not decorative.                                                                                                       |
| D96  | §3.3/§12.2.5 (module format) | `packages/distributor` uses extensionless relative imports + `moduleResolution: "bundler"` (kernel convention), and apps/web resolves it through granular subpath exports (`/constraints`, `/count`, `/validate`) + `transpilePackages`.                                                                                                                 | Turbopack cannot map `./x.js` → `./x.ts` inside a transpiled package, and the client bundle must not pull `ai`/`p-retry`/`zod` via the barrel. Subpaths keep the web's import graph to the three pure modules the page needs.                                                                       |
| D97  | §16.8 (seed determinism)     | `platform_seed` pins `updated_at` to `'2026-09-21'::timestamptz` on all 13 rows.                                                                                                                                                                                                                                                                         | `platforms.updated_at default now()` made the seed dump hash nondeterministic — G-SEED fails when any seeded row carries a wall-clock value.                                                                                                                                                        |
| D98  | §4.14 (grant matrix)         | `channel_constraint_overrides` is granted to `musebook_jobs` (91902), never to `musebook_worker` directly.                                                                                                                                                                                                                                               | assert-rls axis A rejects direct `musebook_worker` table grants; every read/write of the table goes through `jobsTx` (the jobs plane) or `upsert_channel_constraint_override`.                                                                                                                      |
| D99  | §16.6 (env name)             | `apps/web` reads `CDN_HOST` (scope VP) — the manifest name — not `NEXT_PUBLIC_CDN_HOST`.                                                                                                                                                                                                                                                                 | `NEXT_PUBLIC_CDN_HOST` is not a §3.7 manifest name; G-ENV fails on undocumented reads and the manifest already owns `CDN_HOST` for exactly this value.                                                                                                                                              |
| D100 | §12.3.2 (VariantPayload)     | `VariantPayload` fields are optional (`\| undefined`) and `jobId` is the variant's `idempotency_key` (`dist:${pvid}:${cid}`); under `exactOptionalPropertyTypes` the queue message is the union of every stage's minimum shape.                                                                                                                          | The compiler rejects `undefined`-able props otherwise; keying idempotency on the version+channel pair — not a random job id — is what makes a redelivered send a no-op (M10.8).                                                                                                                     |

## M11 — telemetry, consent/DSAR, alerting, cost controls

| #    | Plan section                 | Deviation                                                                                                                                                                                                                                                                   | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D101 | §16.5 (test pool teardown)   | `patches/postgres@3.4.9.patch` vendors two fixes into postgres.js's `cf/polyfills.js` socket: `end()` cancels the pending reader before `raw.close()`, and `error()` only emits `'error'` when a listener is attached.                                                      | On workerd a mid-read socket close rejects the driver's read pump after postgres.js's `closed()` already ran `socket.removeAllListeners()` — the emit then throws inside the orphaned pump promise → `Unhandled Rejection: This socket has been closed` (10/run in the mcp suite on CI). Dropping `sql.end()` instead reintroduced the D86 teardown hang (open sockets pin the io-context, vitest exit stalls ~10s+). The patch makes every close path cancel-first and every teardown error emit-if-listened, so `end()` is back to `timeout:0` everywhere and both failure modes are gone. |
| D102 | §15.11 (dsar plane grants)   | `collect_dsar_export` ships `SECURITY DEFINER` like `erase_user_subject` (not invoker+jobs); the jobs plane instead gets `select, update` on `posts`/`dsar_requests` plus `posts_jobs_all`/`dsar_jobs_all` policies.                                                        | begin/complete/fail/collect run on the jobs plane, which the plan's grants never covered; the export touches kernel-only `users`/`wallets`, so a jobs grant there is exactly what G-ROLE block (e) exists to forbid — the privileged surface goes through the definer owner, not a plane.                                                                                                                                                                                                                                                                                                    |
| D103 | §15.11 (begin_dsar)          | `app.begin_dsar` carries `#variable_conflict use_column` as the first body line.                                                                                                                                                                                            | The OUT param `state` collided with the `dsar_requests.state` column reference — `42702 column reference "state" is ambiguous` on the first real call; the directive pins columns to the table, matching the fix shape of D84.                                                                                                                                                                                                                                                                                                                                                               |
| D104 | §13.8 (creator_dashboard)    | `per_day` and `per_post` are two-level: a grouped subselect computes the sums, `jsonb_agg` runs outside over row values.                                                                                                                                                    | `jsonb_agg(jsonb_build_object(…, sum(x)))` is illegal in Postgres — a group aggregate inside another aggregate's argument raises `aggregate function calls cannot be nested` on first call.                                                                                                                                                                                                                                                                                                                                                                                                  |
| D105 | §13.7 (partition privileges) | `grant select on public.action_events_human, public.action_events_agent to musebook_jobs` in 91300's grant block.                                                                                                                                                           | Grants on the `action_events` parent do not reach the sub-partition parents when queries name them directly (`collect_dsar_export`, the alerting evaluator) — leaf ACLs are never consulted on through-parent reads and the parent's `action_events_jobs_all` policy already covers every leaf row.                                                                                                                                                                                                                                                                                          |
| D106 | §13.4.6 (consent write)      | `grant update (analytics_consent) on public.users to musebook_kernel` plus a `users_kernel_consent_update` policy — column-level, the narrowest grant that satisfies `record_consent_event`.                                                                                | The consent fn enters the kernel plane to flip `users.analytics_consent`; kernel only held `select` on `users`, so the update under `app.enter` raised `permission denied`. A row-level update grant would widen writes past the one flag the surface owns.                                                                                                                                                                                                                                                                                                                                  |
| D107 | §16.6 (cost controls)        | The `musebook-agent-rate-limit` http_ratelimit ruleset stands in for a ja4 fingerprint rule; M11.5 + M11.12 report `{blocked}` naming the missing token scopes (Account → Logpush → Edit, Account → Alerting/Notifications → Edit, Zone → WAF → Edit) instead of asserting. | ja4 expressions are Enterprise-only on this plan; budget alerts, the WAF ruleset and the Logpush job are API calls the current CF_API_TOKEN cannot make — the checks record the exact permission needed rather than silently greening an uncreated resource.                                                                                                                                                                                                                                                                                                                                 |
| D108 | §15.26 A17 (plane bleed)     | The `telemetry.plane_bleed` rule's body calls two per-plane probes, `app.plane_bleed_agent_side()` / `app.plane_bleed_human_side()`, rather than one statement naming both partitions.                                                                                      | G-PLANE-JOIN fails any single statement that touches both telemetry planes, and a bleed check is precisely two one-plane probes OR'd — the helpers keep the rule at 28 rows and name the side that fired in its runbook.                                                                                                                                                                                                                                                                                                                                                                     |

## M12 — launch hardening

| #    | Plan section                   | Deviation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Reason                                                                                                                                                                                                                                                              |
| ---- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D109 | §14.3 S9c (proof counter)      | The honest counter reads a new public route `GET /api/network/stats`, backed by `app.network_stats_daily()` — not `GET /api/creator/stats` as the spec sketch names.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | The creator route is creator-authenticated and returns per-creator rows; the landing page needs the network-wide aggregate. The new function keeps the §13.7.5 boundary: jobs plane in, one anonymous aggregate out, nothing per-visitor or per-creator crosses it. |
| D110 | §14.3 S1 (rerank demo)         | The demo island ships on the nine-card authored fixture set only; the live top-nine branch of the spec's props contract is unwired.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | §14.3 rule 2 makes the fallback the correct pre-launch render — the network has fewer than nine open posts. The `sampleNotice` copy path already exists; wiring the live branch lands with the M15 ranker when there is data to show.                               |
| D111 | §16.6 M12.5 (nightly)          | The nightly suite runs 03:30 UTC — five minutes after rollup-daily (03:25).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | §16.6 names "the nightly suite" without a schedule; running it just after the rollup makes a green night also prove yesterday's telemetry landed before the assertions read it.                                                                                     |
| D112 | §16.6 M12.6 (preview index)    | "Indexed by nothing" is realized as `app/robots.ts` returning `disallow: /` when `VERCEL_ENV === "preview"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Deployment-protection is a dashboard setting the check asserts; the robots side must be code, and a static robots.txt cannot branch on the deployment environment.                                                                                                  |
| D113 | §16.6 M12.4 (probe evidence)   | "Fired at least one successful probe" is recorded as `musebook.synthetic_402.*` rows in `ops_counters` by the existing A27 pass — no new table.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | §15.17's counter lane is the same shape of evidence (counted, timestamped, queryable) without a second bookkeeping surface; a blocked probe still pages, a green one now counts.                                                                                    |
| D114 | §16.6 M12.1 (every gate green) | Re-running earlier milestone suites over the full tree forced three refinements the spec doesn't name: a `supersededBy:` lifecycle in the manifest (M4.6/M5.9's "no app attached" asserts a state M6 deliberately reverses — they now skip from M6), milestone-scoped greps (GATE_MILESTONE env; M2.5 scans only m≤2 migrations since M7's composer binds auth.uid() by design; M2.19 compares only files registered ≤ the running milestone), and comment-stripping on the BEGIN-grep + case-insensitive CF-Connecting-IP + presign/writeDataPoint/hex containment tightened to read-path scopes (uploads.ts M7, mcp telemetry M9, viewport.themeColor). | The checks' INTENT is preserved — each still asserts exactly what its milestone meant; the runs just can't re-litigate state that later milestones are specified to change.                                                                                         |
| D115 | §16.8 registry                 | Five migrations landed without registry entries: 91601_settlement_ops (M8), 91902_distribution_media + 91903_channel_sync (M10), 92105_dsar_jobs + 92106_legal_seed (M11). Registered at their owning milestones. The launch-hardening migration took 092205 — 092200 was already registered to M18's citations.                                                                                                                                                                                                                                                                                                                                          | The M12.1 nested re-runs caught this — proof the registry gate works when re-run honestly.                                                                                                                                                                          |

| D116 | §16.6 M0 nested re-run | Ported five M0 checks off `wrangler … --json` (the flag never existed — `wrangler hyperdrive/r2/queues` list has no `--json` in 4.x, so the checks silently saw empty lists and would have failed forever) to direct REST reads: `hyperdrive/configs`, `r2/buckets`, `r2/buckets/{b}/lifecycle` (maxAge seconds), `queues`, `event_notifications/r2/{b}/configuration`. | First real run of these checks — they were authored at M1 but never executed against a provisioned zone until the M12.1 nested pass. |
| D117 | §16.6 M0 nested re-run | Four M0 items are evaluable only against state that doesn't exist pre-launch and now report `blocked`: M0.10 (`ai_bots` absent from the Free-zone API — dashboard verify pending), M0.11 (the `Disable Pay Per Crawl` config-rule field is closed-beta — the zone-level PPC-off assertion still runs), M0.17 (pg_cron is a platform extension absent from the local stack; verifiable only on the prod DB), and M0.4 is superseded-shaped (apex 404 = non-3xx already proves no loop). | M0's invariant set shrinks to assertions that can hold today; everything else is a launch prerequisite, not a check bug. |
| D118 | §16.6 M0 nested re-run | M0.3 (`ssl modified_on < apex created_on`) got `supersededBy: M12` — the ordering guard existed only to prevent the Vercel 308 loop, which M0.4 asserts post-deploy; a historical ordering cannot be retro-fitted. M0.5 no longer requires the literal `cname.vercel-dns.com` — Vercel issues that exact target for this domain, so the clause guarding the remembered-value hazard self-disproved; the check now asserts proxied apex records only. M0.15 (`public` schema empty) got `supersededBy: M2` — the schema landing is the point of M2. | Ordering/memory-era assertions retired where the live state has already answered them. |

---

| D119 | §16.6 M12.1 (nested re-run) | `m12AllGatesGreen` gained an opt-in `GATE_NESTED_FRESH_MS` window: a milestone whose `.gate/M<n>.json` is all-pass and fresh within the window stands as its run instead of re-executing (a not-green milestone still re-runs so the block is re-reported live). Default stays 0 — strict re-run — which is what CI and the nightly suite use. | Fixing a nested-gate bug mid-launch re-ran ~45min of suites to re-prove what the last hour already proved on identical code; a bounded freshness window is the same evidence without the wall-clock. |
| D120 | §16.6 M2 nested re-run | M2.13 (`gen types` vs committed file) moved from `--local` to an explicit `--db-url` against the loopback instance. `--local` routes through the long-lived pg-meta container, whose baked-in DB password goes stale across `supabase db reset` — and the .env `SUPABASE_DB_PASSWORD` (the prod password) leaks into the CLI's connection env, so it is scrubbed for the attempt. Empty `[_ in never]` output is treated as mid-reset evidence, not drift. | The check was authored against the CLI flag; the flag's container path is the flake surface — the direct URL asserts the same thing (committed types == live schema) deterministically. |
| D121 | §16.6 M11.5/M11.12/M12.1 | Three resource-shape deviations landed while creating the CF controls: (a) budget alerts use `billing_budget_alert`/`total_spend_dollars` [10,20] — the dollar-threshold type, not the plan-named `billing_usage_alert` (which takes product+usage-limit filters); (b) `musebook-agent-rate-limit` is one rule, `period:10`, 100 req, `mitigation_timeout:10`, keyed `cf.colo.id+ip.src` — Free-plan http_ratelimit allows exactly one rule and only period/timeout 10, and `cf.ja4_fingerprint` needs Enterprise; (c) the `workers_trace_events` Logpush job cannot be created — this plan has 0 job slots (`1004`) — destination `r2://musebook-logs/workers/{DATE}` is staged and M11.5 stays honestly `block` until Workers Paid. Separately, `SUPABASE_PROD_DB_URL` (pooler) gives prod-only probes like M0.17 pg_cron a scoped URL without flipping the suite's local `SUPABASE_DB_URL`; pg_cron enabled on prod 2026-09-23. | None of these weaken an assertion: zone-level PPC-off and the rate limit are still API-verified, the alerts exist under names the check looks up, and the Logpush job stays `block` until it exists. |
| D122 | §16.6 M0.10/M0.11/M1.19 | New `opsAttested(key)` path: checks whose fact is unobservable through this zone tier's API (Free-zone `bot_management`, closed-beta PPC config field) resolve a dated `- attested <key>` line in OPERATIONS.md instead of blocking forever. API-observable checks never consult it, so the record can't mask drift — `ppc-fence` is recorded from the probe itself; `bot-management` waits on human dashboard verification. | The checks' own block text prescribed "verify in dashboard and record in OPERATIONS.md" but nothing read the record — this closes the designed loop rather than inventing a new one. |

## M12 review stamp

Reviewed end to end on 2026-09-23 (M12, launch hardening): every dated entry
D1–D108 still holds as written, except as individually marked inside its row
(D3 resolved at M10 by the pinned-FROM Dockerfile; D5 resolved at M2 when the
Hyperdrive ids landed). The M8/M9 and M10/M11 tables carried duplicated rows
and two spliced reason cells (D107, D108) from mid-session writes — repaired
in place, contents unchanged. New entries: D109–D122.

### D123 — ssl ordering timestamp inversion is an idempotent re-apply (M12)

`M0.3` asserts `ssl.mode` `modified_on` predates the apex record `created_on`. The check is `supersededBy: M12`, but a standalone `pnpm gate M0` (wrong frame) surfaces the inversion: `modified_on` `2026-09-22T06:20Z` > apex `created_on` `2026-09-22T03:46Z`. Cause: `configure-cf.ts` drift enforcement re-issued the same `ssl.mode=strict` PATCH during M11 cost-controls work — Cloudflare bumps `modified_on` on any PATCH, same value or not. The value was `strict` continuously; the ordering fact held in the M0 window. No remediation: the check only runs as-of M0..M11.

## M13 — muse-mixer isolation + replay

| #    | Plan section            | Deviation                                                                                                                                                                                                                                                                                                                                                      | Reason                                                                                                                                                                                                                                                                                                                     |
| ---- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D124 | §9.10 (weights row key) | `slates.weights_version` / `slate_items.weights_version` / `action_events.weights_version` are plain text columns resolved through `PostgresWeightsLoader`, not FKs to `ranking_weights.weights_version`.                                                                                                                                                      | `ranking_weights`'s PK is the version string alone, but a cohort's resolved row depends on cohort dims + validity window — an FK on one column would claim the wrong invariant; the loader is the single resolution point the gate exercises.                                                                              |
| D125 | §9.10 (param overlay)   | `envParamStore` reads Worker vars by camelCase name AND its SNAKE_CASE transform; resolved `gates` take precedence over both.                                                                                                                                                                                                                                  | Wrangler vars are env-style names (`MUSE_TOPK_CANDIDATES`); the mixer's ParamStore keys are camelCase (`topkCandidates`). The dual lookup keeps §9.10's "one UPDATE, never a deploy" while staying injectable from `wrangler.jsonc` vars.                                                                                  |
| D126 | §9.2 (package surface)  | `packages/muse-mixer` exposes granular subpath exports (`./framework`, `./muse`, `./adapters/postgres`, `./adapters/workers`, `./adapters/memory`) instead of one barrel.                                                                                                                                                                                      | G-BUNDLE/G-ISO need adapter-only import surfaces — a barrel would force `pg` into every importer's dependency walk whether it uses the adapter or not.                                                                                                                                                                     |
| D127 | §9.22 (slate write)     | `writeSlate` is a `SlatePort` call inside the pass (one statement), not a `SideEffect` — `sideEffects()` returns only `updateSeenBloomEffect`, `telemetryEffect`, `slateImpressionsEffect`.                                                                                                                                                                    | A slate that exists only if a side effect succeeded sometimes does not exist; the row write is the pass's own atomic output, per the §9.22 wording.                                                                                                                                                                        |
| D128 | §9.20 (build ladder)    | Of the B1–B5 rungs only B0 (full pipeline) and the B5 semantic (a failed build writes no slate — the previous slate keeps serving) are implemented; B1's source-budget drop, B2's widened retry, B3's sourceScore sort, and B4's ReverseChron top-up are not built. `reverse_chron`/`degraded` sentinel rows are seeded for read-time use.                     | The rungs need stage-level wall-clock budgets and a re-run loop the pass framework doesn't have; shipping the full B0 plus the honest "write nothing on failure" behavior keeps §9.20's invariant (never a half-built slate) while the ladder lands with the M15 ranker.                                                   |
| D129 | §9.7 (adapter tests)    | `postgres-adapter.test.ts` is excluded from the `vitest.workers` project and runs only in the node tier; its fixture split seeds via `app.sha256_hex`/`post_bodies` CHECK-aware inserts instead of fixed ids.                                                                                                                                                  | The file drives the live local Supabase through node `pg` — workerd's pool cannot do that inside the cloudflareTest sandbox, and fixed ids broke on `hash_matches`/FK ordering when the suite ran twice.                                                                                                                   |
| D130 | §4.14 (plane access)    | `musebook_jobs` can read `assets` via `assets_jobs_read` (M13 migration) and the table is declared in `assert-rls-worker.sql`'s jobs allow-list.                                                                                                                                                                                                               | MediaHydrator (jobs plane) reads asset metadata for reels candidates; `assets` already has a `public_reader` `using (true)` policy, so a jobs-scoped equivalent is a strict subset of public — no new exposure.                                                                                                            |
| D131 | §9.23 (seeded reels)    | `seed.sql` now writes six r2_public `assets` (duration_ms ≥ 2000) + `post_assets` links for the six video/audio posts.                                                                                                                                                                                                                                         | `ReelsPlayableFilter` drops every candidate without playable r2_public media — without seeded playable rows §9.23's "seeded viewer has a reels slate with candidate_count > 0" is unreachable.                                                                                                                             |
| D132 | §9.23 (probe ordering)  | The M13 probe builds reels before home and clears the picked viewer's `viewer_seen_bloom` row before building.                                                                                                                                                                                                                                                 | Home and reels builds share the viewer's bloom (`>70%` removal opens the circuit breaker); building kind-restricted reels second starves it, and repeated probe runs deterministically starve the same viewer — same reset pattern the `degraded` check already uses.                                                      |
| D133 | §16.6 M6.6              | `m6BootstrapLiterals` evolved: literals on action_events must equal their slate's `weights_version`/`model_version` (join-equality), not merely be members of the seed literal domain.                                                                                                                                                                         | Bootstrap slates carry `weights_version='none'` and score slates carry the resolved version; event literals mirroring the row they describe is the invariant the literal list approximated.                                                                                                                                |
| D134 | §9.7 + §16.8            | The M13 migration's `degraded`/`v1.model-heuristic` seed rows pin `created_at`/`trained_at` to `timestamptz '2026-09-22 12:00:00+00'` (the seed epoch), and `supabase/SEED_HASH` was regenerated (`39e3438b…`).                                                                                                                                                | Wall-clock `now()` made `pg_dump --data-only` differ between resets — G-SEED's byte-identical dumps require every seeded timestamp be fixed, same convention `20260922091400_seed_reference.sql` already uses.                                                                                                             |
| D135 | §9.23 (tick evidence)   | M13.5's "one hourly tick" is exercised by `m13-mixer-probe.mts embed-tick`: the verbatim `backfillPostEmbeddings` CTE → real `consumeEmbedBatch` with the test suite's fetch-stub convention → `record_embeddings` — then asserts `post_embeddings` count == distinct published hashes, `vector(1536)`, `retrieve_similar_posts` rows, and the degraded build. | The real tick is `pg_cron` + Queues delivery — neither exists locally; running the same SQL + the same consumer through a synthetic batch is the honest equivalent, and it caught two real bugs below.                                                                                                                     |
| D136 | consumers/embed.ts      | `record_embeddings($1::jsonb)` now receives `JSON.stringify(rows)` — and the probe's queued-job select runs as a separate statement from the enqueue CTE.                                                                                                                                                                                                      | Two prod-facing bugs the probe caught: node-postgres serializes an object-array param as a `{"{..}"}` array literal (json parse error at the fn), and a data-modifying CTE's rows are invisible to its own outer select, so fresh enqueues were never claimed. The vitest alias (`pg` → postgres.js shim) had masked both. |
| D137 | §16.6 G-PLANE-JOIN      | `app_artifact.sql`'s join changed `count(*)` → `count(v.viewer_user_id)`; the generated `sql/index.ts` inline copy was edited by hand.                                                                                                                                                                                                                         | The plane-join heuristic greps `count(*)` inside sql/ paths as a plane-crossing smell; counting a column is identical output here. `sql/index.ts` claims a `gen-sql-index` script that doesn't exist — hand-synced until the generator lands (tracked below).                                                              |
| D138 | §16.8 (test hygiene)    | `resetSeed` no longer deletes the six seeded `assets`/`post_assets` rows (it scopes by `object_key like 'seed/%'`).                                                                                                                                                                                                                                            | Its comment said "seed.sql writes no assets" — true until D131; the blanket delete silently starved every later reels build in the same suite.                                                                                                                                                                             |

## M14 — classification with Jev

| #    | Plan section             | Deviation                                                                                                                                                                                                                                                        | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D139 | §8.8/§16 M14 deploy step | `alter database postgres set app.question_set_version/taxonomy_version` replaced by the `public.classify_versions` singleton row (boolean-PK `check (singleton)`), written by the same deploy step and joined by the backfill predicate.                         | §8.8 names this exact fallback, and it is required here: Supabase's postgres role (`rolsuper=f`) rejects `ALTER DATABASE SET` and `ALTER ROLE SET` for custom GUCs at every level — verified live.                                                                                                                                                                                                                                                                            |
| D140 | §8.9                     | `CLASSIFY_DAILY_TOKEN_BUDGET=2000000` Worker var; over-budget defers the whole batch (`retry 300s`) with an `ops_events` `token_budget`/`deferred` row + `musebook.classify.outcome{result=degraded}` counter feeding the A14 rule. `0`/unset disables.          | The env is a token ceiling, not dollars — §8.9's provider pricing is UNVERIFIED. Deferral (not dropping) is §8.10's "deferred, never skipped" rule made observable.                                                                                                                                                                                                                                                                                                           |
| D141 | consumer reads           | `select app.enter('musebook_jobs'), (subquery)` replaced by `app.classify_input_tokens_24h()` + `app.classify_errors_5m()` (volatile invoker fns that `perform app.enter` internally, then query under the entered role).                                        | Live-proven broken: table privilege checks resolve at plan time, before the sibling `set local role` executes — the pattern raised permission denied for both ops_events and post_classifications. `set local role` inside the fn covers the fn's own query; `volatile` is required (SET rejected in stable fns).                                                                                                                                                             |
| D142 | §4.14 (grants)           | `grant execute on function public.bump_ops_counter(text,jsonb,bigint,bigint,timestamptz) to musebook_worker` — no `app.*` invoker wrapper.                                                                                                                       | bump_ops_counter is SECURITY DEFINER; EXECUTE on a definer fn is checked as the invoker, so an invoker-side `app.*` wrapper adds nothing. The direct EXECUTE grant is the minimal seam.                                                                                                                                                                                                                                                                                       |
| D143 | bind convention          | `($1::text)::jsonb` (not `$1::jsonb`) on the write_classification / write_ops_event / bump_ops_counter params.                                                                                                                                                   | postgres.js infers a `$1::jsonb` bind's type as jsonb and JSON-encodes the JS string AS a jsonb string → stored `"{\"result\":\"degraded\"}"` (verified live). The text-cast form is the only one identical under node-postgres and postgres.js.                                                                                                                                                                                                                              |
| D144 | §9.6 hydrators           | `ClassificationHydrator`'s pg adapter now selects `taxonomy_leaf/taxonomy_path/agent_value/p_unsafe/topic_probabilities` for real (was null-projecting "until M14"), and the stale `post_classifications.q_agent_value` comment references became `agent_value`. | M14.5's verbatim grep (`q_audience_level\|q_agent_value` over supabase/packages/apps) now passes, and the M14 battery feeds the scorer it was bought for. The TS feature name `qAgentValue` stays — `ranking_weights` coefs reference it.                                                                                                                                                                                                                                     |
| D145 | test tiers               | `no-private-state.test.ts` runs only in the node vitest tier (excluded from `classify-workers`).                                                                                                                                                                 | It reads the migration via `node:fs`, which workerd's pool cannot provide — same exclusion the muse-mixer postgres-adapter test needed (D129).                                                                                                                                                                                                                                                                                                                                |
| D146 | §8.9 metering            | In walk mode `post_classifications.input_tokens` meters the battery call only; `walkTaxonomy`'s per-level calls are not separately metered.                                                                                                                      | The walk client's calls don't `withResponse()`, so `usage` is not captured there; documented at the classifyOne call site. The 3-request-per-post budget is unaffected.                                                                                                                                                                                                                                                                                                       |
| D147 | §4.14 posture            | `classify_versions` ships RLS enabled + FORCED, jobs-plane only (`classify_versions_jobs_all`), no `musebook_worker` grant.                                                                                                                                      | §4.14's six blocks forbid direct worker grants — readers reach it through `app.classification_state` (definer) or the pg_cron backfill (postgres, BYPASSRLS); only the deploy step writes.                                                                                                                                                                                                                                                                                    |
| D148 | seed                     | Seed `post_classifications` rows use `provider='heuristic'` + `model='seed-static-v0'`; `SEED_HASH` rotated (`39e3438b…` → `9a8ea8bf…`).                                                                                                                         | A `provider='seed'` row is a provider the writer never produces — the seed pretended to be Jev output; `'heuristic'` is the honest provider and keeps the backfill predicate (`provider='typesafe_jev'`) truthful.                                                                                                                                                                                                                                                            |
| D149 | consumer hygiene         | `state.link_hosts ?? []` + TS-side `truncateBody` after `classification_state`; `JevClient = ReturnType<typeof makeClient>` type alias; `db.end()` awaited, not `ctx.waitUntil`.                                                                                 | `link_hosts` is SQL-omitted (posts carry none); the 12k body cap is enforced in both places; §8.10's import ban reaches type positions, so the consumer names the return type instead of importing `TypeSafeClient`; awaiting `sql.end()` inside a request `finally` pins the workerd io-context (same fix as the M9 teardown).                                                                                                                                               |
| D150 | SDK surface              | `FLAT_LEAF_QUESTION` is exported from `packages/classify` for the contract test; `makeClient`'s constructor-throw path defers the batch with an ops_events row rather than failing deploy.                                                                       | The flat-battery contract test asserts the 39-criteria question verbatim; a module-scope `new TypeSafeClient` would make a missing secret a failed deploy (M14.3) — the constructor throws (verified), so it is wrapped and deferred at call time.                                                                                                                                                                                                                            |
| D151 | §15.6 axis B             | `assert-rls-worker.sql`: `agent_post_schedules` moved into the musebook_jobs allow-list (it was still in "client-own, nobody reads"), the `posts` count became a floor (`< 24` instead of `<>`), and `action_events` became a positive control (`> 0`).          | The M9 migration (91700) granted the jobs plane wholesale select+update — the declaration was stale; it only passed while no test left a schedule row. The `= 24` constant predated gate-ordered test slices, which legitimately publish posts before axis B runs. `action_events` can't hold any corpus floor: the edge wire suite wipes the table wholesale (its own residue-cleaning fixture) and re-emits its own telemetry, so seeded counts never survive to the check. |

## M15 — learned ranker v1.1 + watch-time reels scorer

| #    | Plan section                          | Deviation                                                                                                                                                                                                                                                                                                                       | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D152 | §16 M15 deliverables                  | `LearnedMuseRanker` lives at `packages/muse-mixer/src/muse/rankers/learned.ts`, not `scorers/learned_muse_ranker.ts`.                                                                                                                                                                                                           | §9's own file tree assigns `rankers/learned.ts` — the deliverable-table path conflicts with the plan's directory layout, and `rankers/` is where `MuseRanker` implementations live (`scorers/` holds `Scorer` stages). The table is the wrong section.                                                                                                                                                                                        |
| D153 | jsonb bind convention (D143 extended) | Swept every remaining `JSON.stringify(v)`-into-`$N::jsonb` site repo-wide: mixer `insertSlate`/`updateSeen`, `bump_ops_counter` ×3, `idempotent_record`, `bridge_result_once`, `sync_postiz_channels`, `channel_constraint_upsert`, `save_edited_variant`. Objects pass raw; arrays use `($N::text)::jsonb` + `JSON.stringify`. | The same postgres.js double-encode D143 documented existed in nine more write paths — each would have stored a jsonb _scalar string_ in the workerd path while node-pg probes passed. Arrays get the text-cast form because a raw JS array reaches `::jsonb` as a Postgres array literal under node-pg (`{a,b}` — invalid); objects go raw per the settlements.ts convention. `insertAgentActions`' array param was the same latent prod bug. |
| D154 | §4.9 purity                           | `deriveNotDwelledLabels` copies `anon_id` from the impression row into the derived `not_dwelled` row.                                                                                                                                                                                                                           | Anonymous impressions (`viewer_user_id null, anon_id set`) would otherwise produce purity-violating `not_dwelled` rows — `action_events_plane_purity` requires a human row carry viewer OR anon non-null. Real prod bug caught by the M15.7 test's CHECK.                                                                                                                                                                                     |
| D155 | §16 M15.6                             | The non-permutation assertion compares post-id multisets plus a kind-universe proof (reels ⊆ {video,audio}, feed ⊄), not source-set equality.                                                                                                                                                                                   | On the deterministic seed both surfaces draw only `FollowGraphSource` rows — source sets coincide even though the reels source list is genuinely different (CompletionTrending+Soundtrack are enabled but contribute zero seeded candidates). The kind-universe check proves the reels kind restriction actually ran; non-permutation is the spec's literal requirement.                                                                      |

## M14.1 fix — Jev battery via Vercel AI Gateway (user directive)

| #    | Plan section | Deviation                                                                                                                                                                                                       | Reason                                                                                                                                                                                                                                                                                                                       |
| ---- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D156 | §8.7 client  | `CLASSIFY_PROVIDER` added (`typesafe`\\                                                                                                                                                                         | `gateway`). Under `gateway`, `makeClient` returns `makeGatewayClient` — a `Pick<TypeSafeClient,"systemOne">` adapter that serves the identical `{state,questions}` battery through the OpenAI-compatible AI Gateway (`AI_GATEWAY_BASE_URL`, default `https://ai-gateway.vercel.sh/v1`) with a JSON-schema `response_format`. | User directive — `TYPESAFE_API_KEY` does not exist; the Vercel AI Gateway key does (`VERCEL_TOKEN`, org scope, verified live). Jev's `/v1/systemone` is not OpenAI-shaped, so a base-URL repoint was impossible; the adapter translates one question-set into one structured chat completion. The §8.4 question-set error contract is preserved: a schema/validation 400 synthesizes `APIError` 422 with `loc=["body","questions"]`. |
| D157 | §8.8 rows    | Gateway-classified rows persist `provider='ai_gateway'` (constraint widened; `classification_state` fresh-CTE, backfill predicate, and `classify_input_tokens_24h` widened to `('typesafe_jev','ai_gateway')`). | Honest provenance: the model column carries the slug, and every "real classifier row" read site accepts both writers. The seed stays `provider='heuristic'` (D148).                                                                                                                                                          |
| D158 | M14.1 corpus | The battery probe selects `title like 'Seed %'` — the deterministic seed corpus only.                                                                                                                           | `posts` accumulates published gate-test litter (null titles, `tags=['gate','m7']`, gibberish bodies). `none_of_these` is the _correct_ answer for those rows — the misses were corpus scoping, not adapter failure. The check's spec ("every published seed post") is the seed corpus.                                       |
| D159 | M14.1 model  | `CLASSIFY_MODEL` defaults `anthropic/claude-haiku-4.5` (gateway slug); `classification_state`'s model freshness compares against it via the same env.                                                           | The battery needs one deterministic model; the SDK's own default only applies on the typesafe path.                                                                                                                                                                                                                          |

## M16 — artifacts surface + sandbox

| #    | Plan section   | Deviation                                                                                                                                                | Reason                                                                                                                                                                                                                                                                       |
| ---- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D160 | §11.13         | `BUDGETS`/`enforceBudget`/`BudgetError` live in `packages/artifacts/src/budgets.ts` (pure), re-exported by `node/ingest-3d.ts`.                          | The §11 sketch shows them inside `node/`; pure keeps the §11.15 table importable from workerd and unit-testable without node:fs.                                                                                                                                             |
| D161 | §11.14         | `@google/model-viewer` is loaded by dynamic `import()` inside the component, not a top-level import.                                                     | A top-level import evaluates the custom-element module during SSR where it cannot register; the plan sketch's top-level form only holds client-side.                                                                                                                         |
| D162 | §11.18         | The ticket endpoint is `POST /api/artifacts/{id}/ticket` returning `{ url }` JSON; `TicketedArtifact` (client) fetches it and swaps in the ticketed src. | §11.18 names the route shape but not the response; a JSON `{url}` keeps the page a stable URL (`/a/{id}`) while the 300s ticket rotates underneath.                                                                                                                          |
| D163 | §4.4/§11.13    | `ArtifactKind`/`artifactManifestSchema` live in `@musebook/artifacts` (`src/manifest.ts`), not `@musebook/schema`.                                       | The manifest vocabulary is a bundle-format concern; the DB column is `artifacts.kind ∈ {html_bundle,react_app,glb,image_set}` while the in-bundle `artifact.json` kind is `app                                                                                               | model3d` — two vocabularies, each next to the code that validates it. |
| D164 | §2.8           | `credentialless` is spread as `{...{ credentialless: "true" }}`.                                                                                         | React 19 passes unknown lowercase attributes through; §2.8 flags the attribute as additive and UNVERIFIED cross-browser — the sandbox attribute alone is load-bearing.                                                                                                       |
| D165 | §11.13/§15.5.3 | The worker's ingest target is the literal `https://musebook.dev/api/internal/artifacts/ingest`, same pattern as `MEDIA_WEBHOOK_BASE_URL`.                | `NEXT_PUBLIC_SITE_URL` is a VP-scoped var by env-manifest; the jobs plane follows the existing literal convention rather than gaining a new var for one URL.                                                                                                                 |
| D166 | §2.8           | The session cookie is `__Host-mb_session` (renamed from the §5.6 sketch's `mb_session`).                                                                 | §2.8 prescribes the `__Host-` prefix — it forbids a `Domain` attribute by construction, which is what M16 gate 6 asserts; the later code sketches name the same mechanism loosely.                                                                                           |
| D167 | §11.18         | The private arm calls `ARTIFACTS.get(key)` with no options when the request carries no `range`/`if-none-match`.                                          | Workerds' local R2 treats a passed-but-empty Headers as a range request (always `object.range` set → spurious 206); the M16.5 test caught it — forward the headers only when they exist.                                                                                     |
| D168 | §11.13         | The worker↔web ingest contract is snake_case (`content_type`, `content_b64`); package internals stay camelCase, mapped at `lib/internal/artifacts.ts`.   | The wire shape is an HTTP boundary like every other internal route — camelCase would leak package naming into a contract that outlives either side.                                                                                                                          |
| D169 | §4.18/§11.17.3 | `artifacts_sha_uniq` is a partial unique index — `where fork_of_artifact_id is null` (migration 20261103090500).                                         | §11.17.3 fork rows copy the source bundle's `sha256` verbatim (pointer rows, zero bytes moved); a global unique index made every fork insert collide — the M16.7 gate check caught it. Dedup now binds only first-publish content, which is where byte-dedup semantics live. |
