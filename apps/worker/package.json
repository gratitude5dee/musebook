{
  "name": "musebook-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8789",
    "build": "tsc --build --pretty",
    "typecheck": "tsc --build --pretty",
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run",
    "cf:dry": "wrangler deploy --dry-run --outdir .wrangler/dry",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@musebook/artifacts": "workspace:*",
    "@musebook/classify": "workspace:*",
    "@musebook/connectors": "workspace:*",
    "@musebook/content": "workspace:*",
    "@musebook/distributor": "workspace:*",
    "@musebook/kernel": "workspace:*",
    "@musebook/media": "workspace:*",
    "@musebook/muse-mixer": "workspace:*",
    "@musebook/schema": "workspace:*",
    "pg": "catalog:",
    "@musebook/x402": "workspace:*",
    "fflate": "0.8.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "26.6.2",
    "@types/pg": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "postgres": "3.4.7",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  },
  "exports": {
    "./agent-draft": "./src/cron/agent-draft.ts"
  }
}
